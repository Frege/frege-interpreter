
tasks.compileFrege.dependsOn "compileJava"

compileFrege {
    verbose = true
    hints = true
    make = true
    inline = false
}

dependencies {
    compile project(":frege-interpreter-java-support")
}